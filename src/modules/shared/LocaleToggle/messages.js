import { defineMessages } from 'react-intl';

export const scope = 'LocaleToggle';

export default defineMessages({
  tr: {
    id: `${scope}.tr`,
    defaultMessage: 'tr',
  },
  en: {
    id: `${scope}.en`,
    defaultMessage: 'en',
  },
});
